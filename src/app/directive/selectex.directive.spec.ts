import { SelectexDirective } from './selectex.directive';

describe('SelectexDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectexDirective();
    expect(directive).toBeTruthy();
  });
});
